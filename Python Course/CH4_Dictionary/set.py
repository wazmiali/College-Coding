# set in python set is the collection of unordered  items. each elements in the set must be unique and immutable
collection = {1, 2, 3, 4}

collection.remove(3)
collection.add("Wazmi")
print(collection)
print(type(collection))
# collection.clear()
print(len(collection))
